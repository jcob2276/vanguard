# Vanguard Agent Lessons

Pamięć agenta o napotkanych problemach i ich rozwiązaniach między sesjami. Przed rozpoczęciem każdego zadania sprawdź te wpisy, aby nie powtarzać błędów.

| Data | Zadanie | Problem | Lekcja |
|---|---|---|---|
| 2026-07-03 | Faza 3 CalendarView — fetch events | Supabase query builder zwraca `PromiseLike` (brak metody `.catch()`). | Używaj `async/await` z `try/catch` zamiast `.then().catch()` na builderze lub opakuj w `Promise.resolve()`. |
| 2026-07-03 | SSOT daily_wins + drop project_checkpoints | Zapisy raw do `daily_wins` omijały cache. Brak fizycznego usunięcia tabeli `project_checkpoints`. | Zapisuj tylko przez helpery w `goalSpine.ts`. Ukończ migracje (usunięcie tabel) przed zmianą statusu w dokumentacji. |
| 2026-06-30 | Rollup target_value → goal_kpis | Równoległe modyfikacje `kpi_entries` z JS powodowały race conditions. | Używaj atomowych RPC (np. `increment_kpi_entry_for_week`). Auto-rollup wymaga, aby projekt miał dokładnie 1 KPI. |
| 2026-06-30 | PowerList: sloty i wartości | Git status na starcie sesji pokazywał "clean" mimo niezapisanych zmian roboczych z poprzednich sesji. | Zawsze sprawdzaj rzeczywiste zmiany przez `git diff` na starcie. Stosuj spójny wzorzec kolumn `task_N_*` w `daily_wins`. |
| 2026-06-30 | SpineGuideStrip: fałszywy nudge | Obliczanie spóźnienia refleksji z niepowiązanej tabeli powodowało błędne powiadomienia. | Każdy nudge/overdue musi być liczony bezpośrednio z tabeli, której dotyczy komunikat. |
| 2026-06-27 | NL food accuracy overhaul | Losowe wartości kalorii z LLM przy stałym B/W/T. Słaba dokładność. | Przeprowadź parsowanie przez jeden pipeline, automatycznie zapisuj tylko przy `confidence: high`. |
| 2026-06-28 | Parsowanie gramów posiłków | LLM liczący makro end-to-end generował dryf kaloryczny. | Używaj LLM tylko do ekstrakcji nazwy i wagi, dopasuj do bazy referencyjnej, a LLM stosuj jako fallback. |
| 2026-06-24 | Dodatek CaffeineDecay i wskaźniki | Brak kolumny `caffeine_mg` w tabeli `daily_food_entries`. | Zawsze weryfikuj schemat bazy przed zaplanowaniem prac opartych na "istniejących danych". |
| 2026-06-24 | Typecheck na zmodyfikowanych plikach | `tsc --noEmit` zwraca niezerowy exit code z powodu błędów w innych plikach testowych. | Filtruj błędy po nazwie modyfikowanego pliku, zamiast polegać na samym exit code kompilatora. |
| 2026-06-14 | Weekly Board | Bezpośrednie odejmowanie obiektów `Date` w TS rzuca błędy (TS2362). | Zawsze używaj `.getTime()` do operacji porównania/odejmowania dat w TypeScript. |
| 2026-06-14 | Typowanie opcjonalnych pól | Brak typowania opcjonalnych pól w inline patchach uniemożliwia ich dynamiczne przypisanie. | Jawnie deklaruj typy obiektów modyfikacji (np. `patch: { due_date?: string | null }`). |
| 2026-06-14 | Parametry w komponentach UI | Niezgodność zdefiniowanych propsów z ich wywołaniem (brak opcjonalności parametrów). | Oznaczaj parametry w propsach jako opcjonalne (`param?: type`) lub nadawaj im wartości domyślne. |
| 2026-06-16 | Aktualizacja database.types.ts | Typy z Supabase CLI nie generują się automatycznie po nowej migracji w sesji agenta. | Uruchom `npm run db:update-types` natychmiast po migracji. Jako most tymczasowy używaj `unknown` z type-guardem, NIGDY `as any` (zakaz konstytucji §5). |
| 2026-06-16 | Picker celów w pulpicie | Użycie `as const` na tablicy tupli w `.map()` wywołuje błędy przypisania typu literalnego. | Używaj jawnego typowania tablicy (np. `[string, string, string][]`) zamiast `as const` przy dynamicznej destrukturyzacji. |
| 2026-06-20 | CheckpointsCard | Filtrowanie po statusie `pending`, podczas gdy baza używa statusu `open`. | Przed napisaniem filtru zawsze weryfikuj rzeczywiste wartości enumów w bazie danych. |
| 2026-06-20 | Weryfikacja błędów zapisu | Supabase JS nie zgłasza wyjątków na błędach DB/RLS (zwraca `{ error }` w wyniku), co omijało bloki `catch`. | Zawsze weryfikuj obiekt `error` z wyniku zapisu (używaj helpera `unwrap` z `lib/supabaseUtils.ts`). |
| 2026-06-20 | useHaptics | Hook generował nową referencję funkcji przy każdym renderze, triggerując efekty. | Definiuj hooki bez zależności stanowych poza komponentem lub memoizuj je, aby uniknąć pętli renderów. |
| 2026-06-20 | Formatowanie dat (Warszawa) | Kopiowanie `toLocaleDateString` z timezone ponad 70x powodowało dryf i błędy stref czasowych. | Używaj wyłącznie scentralizowanych helperów z `src/lib/date.ts` (`getTodayWarsaw()`, `formatWarsawDate(d)`). |
| 2026-06-20 | Strefy czasowe w zapytaniach | Porównywanie lokalnego czasu przeglądarki z czasem serwera/Warszawy. | Wszystkie daty we frontendzie i backendzie muszą być zsynchronizowane do `Europe/Warsaw`. |
| 2026-06-20 | Zastosowanie unwrap() | Wrapper `unwrap()` zgłaszał błąd na poprawnych operacjach `update`/`insert` bez `.select()` (brak zwracanych danych). | Używaj `unwrap()` wyłącznie dla zapytań pobierających dane (z `.select()`). Dla zapisu weryfikuj ręcznie `{ error }`. |
| 2026-06-20 | PGRST205 fallback w Keep | Fallback offline mutował tylko stan w pamięci, bez zapisu do localStorage, co cofało edycje po przeładowaniu. | Każda CRUD-ścieżka z trybem fallback (localStorage/offline) musi pisać do tego samego nośnika, z którego jest on odczytywany. |
| 2026-06-20 | Podział monolitów | Wydzielanie podkomponentów z monolitów psuło layout z powodu przenoszenia wrapperów CSS flex/grid. | Pozostawiaj wrapper stylizujący w komponencie nadrzędnym, a do podkomponentu przekazuj czyste dane przez props. |
| 2026-06-20 | Puste catch w ESLint | Puste bloki `catch {}` przy parsowaniu JSON zgłaszają ostrzeżenia ESLint `no-empty`. | Dodaj krótki komentarz wewnątrz bloku `catch` (np. `/* ignore */`), aby linter uznał to za celowe działanie. |
| 2026-06-21 | Logowanie jedzenia z parsera NL | Skalowanie RPC przez `grams/100` powodowało deadlocki przy masowym logowaniu z NL (`Promise.all`). | Przekaż `p_grams=100` (skala 1.0) do RPC. Zapisy wykonuj sekwencyjnie z powodu blokady `pg_advisory_xact_lock`. |
| 2026-06-21 | deepseek-v4-flash a JSON | Model `deepseek-v4-flash` to model reasoning — zwraca content w `reasoning_content`, rzucając błąd przy JSON parserze. | Do ustrukturyzowanych zadań JSON (`responseFormat`) używaj `deepseek-chat` (V3), a nie modeli typu reasoning. |
| 2026-06-21 | Typowanie JSONB w Supabase | Kolumna typu `jsonb` jest typowana w TS jako uniwersalny `Json`, powodując błędy dostępu do właściwości. | Używaj type-guardów i rzutowania (np. `verdict as Record<string, unknown>`) przed odczytem pól z obiektów JSONB. |
| 2026-06-23 | Circular Import w CardFactory | Importowanie ciężkiego `CardFactory` wewnątrz komponentów czatu spowalniało bundle i groziło zapętleniem. | Stosuj dynamiczny import (`import(...)` w `useEffect`) dla ciężkich komponentów typu factory. |
| 2026-06-23 | deepseekChat apiKey | Edge function crashowało przy wywołaniu `deepseekChat()`, które nie czytało klucza z env automatycznie. | Zawsze jawnie przekazuj `apiKey: Deno.env.get('DEEPSEEK_API_KEY')` do wywołania helpera. |
| 2026-06-23 | ScheduleView offline storage | Brak tabeli w bazie danych na widok harmonogramu. | Zastosowano persystencję w localStorage i czyste funkcje transformacji czasu - najprostszy stabilny schemat. |
| 2026-06-24 | Planowanie duplikatów algorytmów | Zaplanowanie od zera implementacji wskaźników, które fizycznie istniały w codebase w innej lokalizacji. | Przed rozpoczęciem prac zrób pełny grep po bazie i kodzie pod kątem kluczowych słów powiązanych z funkcją. |
| 2026-06-29 | Usuwanie planowania z Telegrama | Pozostawienie martwych handlerów i routerów przechwytywało głosówki użytkownika przeznaczone dla refleksji. | Usuwając funkcję, bezwzględnie usuń powiązane z nią punkty wejścia, routery i callbacki w tej samej sesji. |
| 2026-06-29 | Spójność goalSpine | Równoległe zapytania i upserty omijały centralne cache `goalSpine.ts`, rozjeżdżając interfejs z Wyrocznią. | Cała logika celów i zadań musi przechodzić przez jeden punkt dostępu `goalSpine.ts` (fetch + invalidacja). |
| 2026-06-29 | Pętla renderowania useDirectionContext | Dynamiczne tworzenie novych referencji obiektu w zależnościach efektów wywoływało pętlę renderów. | Używaj stałych referencji (np. `EMPTY_OBJECT`), memoizacji oraz refs w hookach invalidacji. |
| 2026-07-12 | HTTP timeouty w integracjach | Każdy klient HTTP miał własne parametry timeoutów i brak retry na błędy sieciowe. | Wprowadzono `fetchWithRetry` w `_shared/httpClient.ts` (wykładniczy backoff na błędy sieciowe/429/50x). |
| 2026-06-29 | Martwy kod po wdrożeniach | Pozostawianie nieużywanych wersji komponentów po wybraniu nowego wariantu projektu. | Usuwaj stare wersje plików w tym samym commicie, w którym podłączasz nowy wariant. |
| 2026-06-30 | Migracje usuwające kolumny | Usunięcie kolumny `task_N_checkpoint_id` popsuło frontend, który rzadko (ale jednak) z niej korzystał. | Przed usunięciem kolumny upewnij się za pomocą grep, że nie ma do niej żadnych odwołań w kodzie UI (nie tylko w bazie). |
| 2026-07-02 | Formatowanie backlogu | Ignorowanie kryteriów i formatowania podanego przez użytkownika na rzecz własnych wzorców. | Zawsze stosuj strukturę priorytetów (S/A/B) i kryteria sortowania wskazane przez użytkownika. |
| 2026-07-02 | Unikanie implementacji interwencyjnych | Odrzucanie funkcji automatyzacji planowania jako "scope creep" na rzecz pasywnego wyświetlania danych. | Jeśli użytkownik zmienia zasady konstytucyjne, zmień podejście i wdrażaj aktywne mechanizmy modyfikacji stanu. |
| 2026-07-02 | Dodatkowe integracje | Proponowanie integracji zewnętrznych (np. YNAB, copy-paste) bez realnego uzasadnienia i zysku dla systemu. | Odrzucaj pomysły o słabej dźwigni integracyjnej. Zawsze sprawdzaj, czy dana domena ma już swój odpowiednik w repo. |
| 2026-07-01 | Leniwe ładowanie a płynność UI | Leniwe montowanie widoków i podzakładek wymuszało pobieranie danych przy każdym kliknięciu. | Pre-montuj krytyczne ekrany przy starcie i przełączaj je klasą `hidden` w CSS, zapewniając czas reakcji 0ms. |
| 2026-07-04 | Duplikacja zapisu w MorningPlanModal | Zapisywanie do `daily_wins` z pominięciem helperów omijało invalidację cache i psuło stan widoków. | Zawsze sprawdzaj poprawność nowego modułu z listą kanonicznych helperów przed wdrożeniem. |
| 2026-07-04 | Heksagon i sfery życia | Propozycja nowej tabeli dla 6 sfer, podczas gdy istniał już system budżetów czasowych o innej taksonomii. | Przed dodaniem "kategorii" sprawdź istniejące budżety i taksonomie. Scalaj powiązane tabele przed rozbudową. |
| 2026-07-04 | Wzorzec cichego błędu w cronach | Cron zgłaszał sukces (`200 OK`), chociaż baza danych odrzuciła insert z powodu check constraint. | Zawsze weryfikuj stan faktyczny w bazie danych (`SELECT count(*)`) po teście, a nie tylko odpowiedź JSON API. |
| 2026-07-04 | Puste pola w life_goals | Nudge odsyłał do zakładki Projekty, gdzie nie było formularza zapisu do tabeli sprawdzanej przez nudge. | Upewnij się, że ekrany docelowe posiadają fizyczną ścieżkę zapisu do tabel sprawdzanych przez reguły nudges. |
| 2026-07-04 | Usunięcie podzadań w TodoCard | Dwa równoległe systemy podzadań (checklist w notatce vs parent_task_id) dublowały logikę. | Przed migracją sprawdź krytyczne zapełnienie. Usuń martwy format i przepnij kreatory (np. AI breakdown). |
| 2026-07-04 | Refleksje w syntezie tygodniowej | Ignorowanie istniejącej funkcji `vanguard-week-recap` przy wdrażaniu nowej analizy refleksji tygodniowej. | Zawsze zgrepuj funkcje edge pod kątem nazwy tabeli przed wdrożeniem nowego procesu analitycznego. |
| 2026-07-08 | Brier score i predykcje | Brak generowania typów w TS po dodaniu nowej tabeli blokował kompilację DAL. | Wykonaj `npm run db:update-types` natychmiast po migracji dodającej tabele, by zapobiec błędom TS. |
| 2026-07-08 | Wywołania usuniętych tabel | Funkcja DB `deprecate_superseded_facts` odwoływała się do zdeprecjonowanej, nieistniejącej tabeli. | Testuj funkcje SQL bezpośrednio w bazie za pomocą transakcji testowych przed ich wdrożeniem na produkcji. |
| 2026-07-08 | SB_SECRET_KEY w lokalnym CLI | Brak zmiennej `SB_SECRET_KEY` w środowisku lokalnym CLI Supabase powodował błędy 401. | Stosuj fallback `SB_SECRET_KEY \|\| SUPABASE_SERVICE_ROLE_KEY` przy weryfikacji tokenów w Deno. |
| 2026-07-08 | Wielowarstwowy RAG | Pobieranie wszystkich typów pamięci w jednym ciężkim zapytaniu spowalniało odpowiedź o 2-3s. | Rozdziel pobieranie RAG na ustrukturyzowane, niezależne kroki i stosuj sztywne limity znaków (character budget). |
| 2026-07-08 | Moduły współdzielone w Deno | Importy lokalne bez rozszerzeń powodowały błędy Deno (bezwzględny wymóg `.ts`). | W plikach pakietów współdzielonych (np. packages/domain) zawsze stosuj jawne rozszerzenia plików w importach. |
| 2026-07-08 | Konsolidacja sync i cronów | Usunięcie endpointu edge function bez wyczyszczenia powiązanego zadania pg_cron powodowało błędy 404. | Przed usunięciem endpointu sprawdź tabelę `cron.job` i wyczyść powiązany harmonogram za pomocą `cron.unschedule`. |
| 2026-07-08 | Re-read body stream w Deno | Próba odczytania ciała żądania (`req.json()`) w routerze i pod-handlerze rzucała błąd (body already read). | Przy routowaniu przekazuj gotowy obiekt żądania ze zrekonstruowanym body lub przekazuj sparsowany obiekt. |
| 2026-07-08 | Wdrożenia a dryf kodu | Funkcje produkcyjne w bazie nie miały kodu w repo, a deploy-guard nie wykrywał dryfu kodu na serwerze. | Używaj `supabase functions list` do wykrywania starych funkcji (duchów), które zostały usunięte z repo. |
| 2026-07-10 | Starzenie się dokumentacji | Dokumentacja opisująca stan (np. lista plików) szybko się dezaktualizuje. | Opisuj mechanizmy (lintery, testy), a nie stany kodu. Dodawaj datę ostatniej weryfikacji do plików docs. |
| 2026-07-11 | Duplikacja zapytań o obciążenia | Trzy niezależne wywołania wyliczały strain z różną logiką invalidacji i brakiem cache. | Skonsoliduj wyliczenia do jednego helpera. Wyniki ciężkich analiz (np. korelacji) czytaj z cache, nie licz w runtime. |
| 2026-07-11 | Knip a pliki w budowie | Knip usuwał nowo dodane pliki repozytoriów, które nie miały jeszcze konsumentów w kodzie. | Dodawaj nowo tworzone, typowane foldery i pliki do listy `ignore` w `knip.json` przed rozpoczęciem prac. |
| 2026-07-11 | deploy-guard na Windows | SQL przekazywany jako string przez `execSync` crashował w PowerShellu na znakach nowej linii. | SQL do CLI przekazuj wyłącznie zapisując go do pliku tymczasowego i stosując flagę `--file`. |
| 2026-07-13 | Incydent check constraint | Baza danych odrzucała insert z `event_kind='recovery_event'` z powodu braku wartości w check constraint. | Clampuj i mapuj dynamiczne wartości z LLM na dozwolone przez DB przed wykonaniem zapytania. |
| 2026-07-19 | Path to 9 / Oracle worldState | `sanitizeStateVector` trzymał stare klucze (`today_plan`, `goal_spine`…) — `fetchWorldState` bipł 6 tabel, a prompt dostawał `"{}"`. | Allowlist sanitize musi mirrorować realny kształt `WorldState` (`biometrics/execution/…`). Po zmianie shape — grep `STATE_VECTOR_KEYS` i test że prompt nie jest pusty. |
| 2026-07-19 | Ocena vs backlog | Ocena apki z BACKLOG.md zaniżyła claims/backfill — kod miał już triggery i date ranges. | Przy ocenie architektury czytaj kod (grep insert/select), nie backlog. Backlog = kolejka, nie stan live. |
| 2026-07-19 | Close domain seams | Partial failure morning planu (wins + slots + todo schedule osobno). Blok komentarza `*/` w ścieżce `hooks` zamykał JSDoc w środku pliku. | Multi-table day write → jedna RPC. W komentarzach unikaj `*/` w ścieżkach plików (używaj `hooks` albo backticków). |
| 2026-07-19 | WeeklyBodyPulse | Pulse ciała pokazywał abstrakcyjny recovery + „dni z limitem” zamiast przebiegu aktywności. | Zakładka Tydzień = rolling 7 dni: siła / bieg / sauna + sen (śr, zasypianie/wstawanie, deep/REM, efektywność, HRV, best/worst) z Oura; recovery tylko jako dopisek. |
| 2026-07-19 | WeeklyNutritionPulse | Odżywianie pokazywało tylko dni/białko/budżet. | Rolling 7 dni z `daily_nutrition`: kcal, B/W/T, jakość, błonnik, cukier; średnie tylko z zapisanych dni. |
| 2026-07-19 | Tydzień w liczbach | Block2WeekStats dublował ProjectWeekKpis + pulse ciała/odżywiania w tym samym flow. | Jedna powierzchnia na fakt: KPI w zoom-out/recap; biometria w pulse. Martwy fetch weekFacts też wyciąć z DAL. |
| 2026-07-19 | Refleksja 7→5 | Q2/Q4/Q6 dublowały winę/unik/żal; UI miało 7 slotów. | Zostaw: zejść z głowy, plan vs wykonanie, unik, energia, belief. `proud_of`/`week_regret` zostają w DB (load+save), znikają z formularza. |
| 2026-07-20 | Szkielet sekcji Finanse | Nowa domena mogła niepotrzebnie wejść w stan i dane głównego dashboardu. | Nowe top-level domeny zaczynaj jako lazy route z własnym modułem; dashboard powinien dostarczać tylko punkt wejścia. |
| 2026-07-20 | Start dnia — konflikt tytułu Todo | Odtworzenie `daily_win_tasks` uruchamiało trigger zapisujący snapshot tytułu z powrotem do kanonicznego `todo_items`, co łamało unikalność. | Trigger synchronizujący projekcję z encją źródłową nie może działać przy INSERT-cie projekcji; zapis zwrotny tylko po jawnej edycji. |
| 2026-07-20 | Start dnia — komunikat z Power List | `Błąd startu dnia` pochodzi z `usePowerListActions.startNewDay`, nie z modala Morning Plan. Duplikat `(user_id, section_id, title)` pada też przy `materializeDailyWinTodos` → `createTodoItem`. | Przed INSERT szukaj otwartego todo w sekcji; przy re-startcie dnia wyczyść `daily_win_tasks` przed ponownym insertem. |
| 2026-07-20 | Moduł Finanse — FIRE + decyzje | Pełny pionowy plaster: `packages/domain/finance/*`, migracja `finance_*`, `financeApi.ts`, `/finanse` z zakładkami. Bez AI advisor (guardrails). | Domain math bez React; dane przez `financeApi`; kalkulator ≠ ewidencja jak Kontomierz. |
| 2026-07-20 | Terminy / life admin | Urodziny i polisy to nie todo (completion ≠ „zrobione na zawsze”) i nie finanse (nie zawsze mają kwotę). | Osobna tabela `life_obligations` + lead_offsets; push w istniejącym `vanguard-push-reminder`; UI `/terminy` w Narzędziach. |
| 2026-07-20 | Terminy Life Horizon | Pierwszy CRUD wyglądał jak admin form (inline + empty naraz). | Horizon hero + urgency rails + vault tabs; add tylko w Modal; shell jak Finanse; derived buckets testowalne. |
