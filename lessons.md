# Vanguard Agent Lessons

Pamięć agenta o napotkanych problemach i ich rozwiązaniach między sesjami. Przed rozpoczęciem każdego zadania sprawdź te wpisy, aby nie powtarzać błędów.

| Data | Zadanie | Problem | Lekcja |
|---|---|---|---|
| 2026-07-03 | Faza 3 CalendarView — fetch events | Supabase builder `.then().catch()` nie działa bo `.then()` zwraca `PromiseLike`, nie pełny `Promise`. Błąd TS: `Property 'catch' does not exist on PromiseLike`. | Zawsze używaj `async/await` z `try/catch` przy supabase fetch, nie chainuj `.then().catch()` na builderze. Alternatywnie: `Promise.resolve(query).then(...).catch(...)`. |
| 2026-07-03 | SSOT daily_wins + drop project_checkpoints | PowerList/Direction pisały raw do `daily_wins` bez `invalidateGoalSpineCache()` — SpineGuideStrip lagował. `project_checkpoints` bez DROP w repo mimo claimu w FEATURE_LIFECYCLE. | Kanoniczne zapisy: `updateDailyWin` / `insertDailyWin` / `markDailyWinsPartial` w `goalSpine.ts` + `useDashboardData` nasłuchuje invalidacji. Migracja `20260630240000`: FK `task_N_checkpoint_id` → `todo_items`, DROP `project_checkpoints`. Direction monolit (10× fetch) — świadomie odłożony; zapisy już przez spine. |
| 2026-06-30 | Domknięcie pętli dzień→tydzień: rollup target_value → goal_kpis | `kpi_entries` ma jedną kolumnę `value` per `(kpi_id, week_start)`, więc równoległe odznaczanie/cofanie zadań (toggleTask) mogłoby się gubić przy read-modify-write z JS. KPI tworzone dotąd w `WeeklyReview.tsx` (`addKpi`) nigdy nie ustawiały `project_id` — istniał kolumna, ale żaden flow jej nie wypełniał. | Atomowy `increment_kpi_entry_for_week` RPC (`ON CONFLICT...DO UPDATE SET value = coalesce + excluded`) zamiast JS read-modify-write — bezpieczne przy szybkim zaznacz/odznacz. Auto-rollup tylko gdy projekt ma DOKŁADNIE 1 KPI (inaczej niejednoznaczne, pomijane) — `rollupTaskCompletion()` w `goalSpine.ts` to czysta, testowalna funkcja decyzyjna, wzorem `dayWinStateFromRow`. |
| 2026-06-30 | PowerList: ilość + pora dnia per zadanie (task_N_target_value/time_slot) | `git status` na starcie sesji pokazywał "clean", ale `PowerList.tsx` miał niescommitowane lokalne zmiany (Cursor dodał pola do `TaskSlot` bez ich wpięcia do UI/DB) — `gitStatus` w system-reminder to snapshot sprzed sesji, nie live state. | Przed oceną "co zrobił inny agent" zrobić `git stash`+porównanie albo `git diff` na żywo, nie ufać banerowi z początku rozmowy. Wzorzec kolumn `task_N_*` w `daily_wins` (per-task, nie JSONB) — trzymać się go przy kolejnych polach per-slot, żeby nie mieszać dwóch konwencji. |
| 2026-06-30 | SpineGuideStrip: fałszywy nudge "Dawno nie było refleksji tygodnia" mimo 2x zrobionego review | `goalSpineGuide.ts` gate'ował komunikat o refleksji tygodnia (`weekly_reviews.review_completed_at`) przez `kpiOverdueDays` — liczbę dni od ostatniego wpisu w ZUPEŁNIE INNEJ tabeli `weekly_kpi_reviews` (osobna funkcja "KPI review", nigdy nie wypełniana). Dokładnie ten sam "dwa równoległe weekly review" duplikat co w [[project_plan_module]]. | Każdy nudge/cue musi liczyć overdue z TEJ SAMEJ tabeli, której dotyczy tekst komunikatu. Dodałem `fetchLatestCompletedWeeklyReviewDate()` (filtr `review_completed_at is not null`, nie najnowszy `week_start` — bieżący tydzień może mieć pusty wiersz) i osobne pole `weekReflectionOverdueDays` w `SpineGuideContext`, odseparowane od `kpiOverdueDays`. Przy dodawaniu nowego "overdue X" sprawdzić, czy faktycznie liczy się od ostatniego completion TEGO X, nie pokrewnej tabeli. |
| 2026-06-27 | NL food accuracy overhaul | LLM: losowe kcal przy tym samym B/W/T; słabe dopasowanie OFF; Telegram bez podglądu; poprawki tylko w prompcie. | `finalizeParsedItems` = jeden pipeline; auto-save tylko `confidence: high`; GENERIC SSOT w `foodGeneric.ts`; Telegram → `food_parse_pending` + inline Zapisz. |
| 2026-06-28 | Unbreakable food parse (grams-only) | LLM liczył makro end-to-end → dryfy kcal; brak provenance w DB. | `parseMealText` = tylko name+grams; reconcile → `food_reference_pl` / generic / OFF; `fillMacrosLlmFallback` tylko dla unmatched; `parse_meta` w `daily_food_entries`; migracja `20260628100000`. |
| 2026-06-24 | Etap 7-8 drobnych dodatków (CaffeineDecay, FitnessAge, VitalityEngine, UX pills) | `daily_food_entries` nie ma kolumny `caffeine_mg` — plan zakładał "dane już są" ale były tylko nazwy produktów. | Infer caffeine z nazwy (`estimateCaffeineMg()`) + `logged_at` timestamp — wystarczające dla osobistego systemu. Przy nowych "dane już są" sprawdzać schemat tabeli PRZED planowaniem. |
| 2026-06-24 | Etap 8 UX — TypeScript check na zmienionych plikach | `tsc --noEmit` zwraca exit code 2 nawet gdy błąd jest wyłącznie w testowym fixture innego pliku. | Filtrować output przez nazwę pliku, nie polegać na exit code — `No errors in modified files` = czyste. Pre-existing błędy w testach nie blokują frontendu. |
| 2026-06-14 | Weekly Board implementation | Arithmetic subtraction of Date objects directly causes type errors in TypeScript (TS2362 / TS2363). | Zawsze używaj `.getTime()` przy odejmowaniu lub sortowaniu obiektów `Date` w TS. |
| 2026-06-14 | Weekly Board implementation | Type inference of inline declared patch object blocks dynamic assignment of optional properties (like `due_date` when defined initially without it). | Zadeklaruj jawny typ obiektu (np. `patch: { ai_bucket: string; due_date?: string | null }`) przy tworzeniu patcha. |
| 2026-06-14 | UI component compile validation | `SectionTitle` component defined parameters mismatching inline props invocation without `action`. | Zawsze w definicji propsów komponentu w TS zaznaczaj parametry jako opcjonalne (`action?: any`) lub dodawaj wartości domyślne. |
| 2026-06-16 | Dodanie kolumny `life_goal` do `dreams` przez MCP migration | `database.types.ts` NIE jest automatycznie regenerowany po migracji — TS rzuca błąd `not assignable to type 'never'` na nowych polach. | Castuj payload insertu/updatu jako `as any` do czasu ręcznej regeneracji typów (`supabase gen types`). |
| 2026-06-16 | Picker celów życiowych w DesktopDashboard | `as const` na tablicy tupli wewnątrz `.map()` powoduje TS2322 bo destrukturyzacja nie może przypisać do literal type. | Zamiast `as const` używaj jawnego typu `[string, string, string][]`. |
| 2026-06-20 | CheckpointsCard w Dziś | Checkpointy miały `status = 'open'` w DB ale karta filtrowała `.eq('status', 'pending')` — karta zawsze była pusta. | Sprawdzaj rzeczywiste wartości enum w DB zanim piszesz filtr statusu; użyj `.in('status', ['pending', 'open'])` jako bezpieczna forma. |
| 2026-06-20 | `/goal` pełny przegląd kodu/jakości | `MiddayCheckInCard.save()` robił `await upsert(...)` w `try` i zawsze ustawiał `setSaved(true)`, bo Supabase JS nie throwuje na błędzie Postgres/RLS — zwraca `{error}` w resolved value. Catch nigdy się nie odpalał, użytkownik widział "zapisano" nawet gdy nic się nie zapisało. | Po `supabase....().upsert/insert/update()` zawsze sprawdzaj `error` z wyniku (użyj `unwrap()` z `lib/supabaseUtils.ts`, nie goły `try/catch` — ten łapie tylko network-level throws, nie DB errors). |
| 2026-06-20 | `/goal` pełny przegląd kodu/jakości | `useHaptics()` zwracał nowy obiekt+funkcje przy każdym renderze → każdy komponent z `haptics` w deps `useEffect` dostawał `exhaustive-deps` warning i efekt re-truthował się bez sensu. | Hooki bez zależności od props/state (haptics, helpers) zwracaj jako stały obiekt zdefiniowany raz na module-level, nie twórz go w body hooka. |
| 2026-06-20 | `/goal` pełny przegląd kodu/jakości | `new Date().toLocaleDateString('en-CA', { timeZone: 'Europe/Warsaw' })` był copy-pasted ~70x w 25 plikach frontu — BUG-01 (zły timezone w `tomorrowWarsawDate`) pokazał już, że to miejsce najwyższego ryzyka regresji. | Dodano `src/lib/date.ts` (`getTodayWarsaw()`, `formatWarsawDate(d)`) i podpięto wszystkie miejsca. Nowy kod z datą Warsaw zawsze przez ten helper, nigdy inline `toLocaleDateString`. |
| 2026-06-20 | `/goal` deep bugfix pass (frontend+backend, 8 parallel review agentów) | Wiele miejsc liczyło "dziś"/zakresy dat z `new Date()` (czas lokalny browsera/serwera, nie Warsaw) i porównywało wynik z resztą apki, która jest Warsaw-anchored — `Direction.tsx` miał DWIE konkurujące funkcje `todayDate()`/`todayWarsaw()` jednocześnie używane w tym samym komponencie. `DesktopDashboard.tsx`: `body.find(b => b.date <= daysBefore(28))` na tablicy ASC zwracał najstarszy wpis (~90 dni), nie ten najbliższy 28 dniom — klasyczny błąd `.find()` na rosnącej liście przy szukaniu "najbliższego do cutoffu". | Każda funkcja licząca "dziś"/zakres dat musi przejść przez `getTodayWarsaw()`/`formatWarsawDate()` — zero wyjątków, zero równoległych lokalnych helperów. Szukając "najbliższego elementu przed/po cutoffie" w tablicy sortowanej ASC, użyj `.reverse().find()` albo iteruj od końca, nigdy `.find()` na surowej tablicy. |
| 2026-06-20 | `/goal` deep bugfix pass | Supabase `.update()/.insert()` bez `.select()` NIE da się bezpiecznie sprawdzić przez `unwrap()` z `lib/supabaseUtils.ts` — `unwrap` rzuca "No data returned" gdy `data` jest `null`, co dla plain update/insert (bez `.select()`) jest NORMALNYM wynikiem sukcesu, nie błędem. `unwrap`/`unwrapList` są tylko dla zapytań z `.select()`. | Dla zapisów bez `.select()` sprawdzaj `{ error }` ręcznie (`if (error) throw new Error(error.message)`), nie owijaj w `unwrap()`. |
| 2026-06-20 | `/goal` deep bugfix pass (Keep.tsx PGRST205 fallback) | `handleUpdate`/`handleDelete`/`handleTogglePin` w Keep.tsx ignorowały `PGRST205` (tabela nie istnieje) i mutowały tylko `setNotes()` w pamięci — bez zapisu do `localStorage('vanguard_local_keep_notes')`. Po odświeżeniu `fetchNotes()` czytał starą wersję z localStorage — edycje/usunięcia "wracały" po reload. | Każda CRUD-ścieżka z trybem fallback (localStorage/offline) musi pisać do TEGO SAMEGO storage co ścieżka odczytu (`fetchNotes`), nie tylko do React state — inaczej fallback jest tylko częściowy i myli użytkownika. |
| 2026-06-20 | Monolith refactoring (`Dashboard.tsx`, `Stats.tsx`) | Refaktoryzacja ogromnych plików poprzez wydzielanie podkomponentów (np. `NutritionCard`) pozwala uprościć strukturę. Zachowanie kontenerów wrapperów layoutu w nadrzędnym pliku pozwala uniknąć zmian stylizacyjnych i zachować pixel-perfect wygląd. | Zawsze pozostawiaj kontenery stylizujące (np. flex/grid/spacing wrappers) w głównym komponencie sterującym, a same logiczne widgety wydzielaj jako pure components zasilane przez props. |
| 2026-06-20 | ESLint empty catch blocks | Puste bloki `catch {}` przy parsowaniu JSON (np. w preferencjach profilu) powodują błędy `no-empty` w ESLint przy rygorystycznym `--max-warnings=0`. | Zawsze wstawiaj komentarz (np. `catch { /* ignore malformed pref */ }`) wewnątrz pustych bloków `catch`, aby linter traktował blok jako celowo udokumentowany, a nie pusty. |
| 2026-06-21 | NL food logging (parse-food-nl) | `add_food_entry` RPC skaluje makra przez `grams/100`. Przy NL loggingu użytkownik wpisuje wartości na porcję, nie na 100g. Aby ominąć skalowanie bez nowej migracji: przekaż `p_grams=100` → scale factor=1.0 → wartości przechodzą bez zmian. Wpisy z NL mode mają `amount="100 g"` co jest nieco mylące, ale akceptowalne. | Dla bulk NL entries: wykonuj `add_food_entry` sekwencyjnie (nie `Promise.all`), bo RPC używa `pg_advisory_xact_lock` na `(user_id, date)` — równoległe wywołania na ten sam klucz powodują deadlock. |
| 2026-06-21 | deepseek-v4-flash zwraca pusty content | `deepseek-v4-flash` to model reasoning — wynik idzie do `reasoning_content`, a `message.content` jest `""`. `deepseekChat()` ekstraukuje tylko `content`, więc każda próba parsowania kończy się błędem. | Dla zadań wymagających `json_object` lub deterministic output używaj `deepseek-chat` (V3). Modele reasoning (`v4-flash`, `deepseek-reasoner`) działają przez inny `_shared/deepseek.ts` który odczytuje `reasoning_content` — nie nadają się do `responseFormat: json_object` na obecnym helperze. |
| 2026-06-21 | Supabase JSONB verdict typing | `nutrition_targets.verdict` ma typ `jsonb` → w TS wychodzi jako `Json` (union string|number|bool|object|array). Dostęp do pola `verdict.food_suggestions` bez guardy rzuca TS2339. | Castuj przez guard: `const v = verdict && typeof verdict === 'object' && !Array.isArray(verdict) ? verdict as Record<string, unknown> : undefined` przed odczytem pól. |
| 2026-06-23 | Memex Faza 5 — CardFactory circular import | `AiCardRenderer` w `ChatItems.tsx` importuje `CardFactory` który importuje wiele kart — potencjalny problem przy SSR/tree-shaking. Rozwiązanie: dynamic import (`import('../cards/CardFactory')`) w `useEffect` wewnątrz komponentu. | W komponentach ChatItems używaj dynamic import dla ciężkich factory-like modułów, żeby nie blokować initial bundle. |
| 2026-06-23 | Memex Faza 8 — contextCompression deepseekChat signature | `deepseekChat()` wymaga jawnego `apiKey` jako parametru — nie czyta z env automatycznie. | Zawsze przekaż `apiKey: Deno.env.get('DEEPSEEK_API_KEY') ?? ''` do `deepseekChat()` w edge functions. |
| 2026-06-23 | Memex Faza 6 — ScheduleViewData localStorage | ScheduleView nie ma własnej tabeli Supabase — persystuje w `localStorage('vanguard_schedule_view')`. `sweepPastEventsInState` jest czystą funkcją — testowalna bez Supabase. | Dla client-side only state (brak wymogu sync między urządzeniami) localStorage + czyst funkcja transformacji = najprostsze rozwiązanie bez migracji. |
| 2026-06-24 | Plan `docs/PLAN_READINESS_NOOP.md` (port algorytmów Noop/Strand) | Zaplanowano "od zera" Baselines (EWMA)/RecoveryScorer/ReadinessEngine jako nowe Etapy 2-3, zanim sprawdzono `compute-daily-strain/index.ts` — okazało się, że te trzy porty NOOP **już tam żyją** (komentarze w kodzie explicite mówią "NOOP port"), licząc `daily_strain.recovery_score`/`readiness_level` od dawna. Research repo źródłowego (Strand) bez audytu WŁASNEGO kodu/bazy prowadzi do planowania duplikatów. | Przed pisaniem planu portującego algorytmy/funkcje z zewnętrznego źródła: najpierw `grep`/Supabase MCP `list_tables`+`execute_sql` po WŁASNYM repo i bazie pod kluczowe nazwy koncepcji (recovery, baseline, readiness itd.) — dopiero potem projektować nowe etapy. Dotyczy też tabel-widoków (`oura_hr_zones_daily` to VIEW, nie pojawia się w `list_tables` bez `table_type` filtra — sprawdzaj `information_schema.tables` gdy podejrzewasz VIEW). |
| 2026-06-29 | Telegram planning removal | Router Telegram miał naraz reflection + planning + rescue, przez co zwykłe głosówki wpadały w fallback refleksji ("Refleksja zapisana...") i myliły tryby. Dodatkowo handler map w README zawierał nieistniejące pliki (`morning.ts`, `midday.ts`). | Gdy decyzja produktowa brzmi "planning tylko w aplikacji", trzeba od razu usunąć cały routing planningu/callbacki/handlery z Telegrama oraz zwęzić okno pending reflection (np. 6h). Inaczej martwe gałęzie dalej przechwytują głosówki mimo poprawnej architektury na papierze. |
| 2026-06-29 | goalSpine + SpineGuideStrip | Równoległe odczyty `weekly_reviews`/`sprint_goals` z wielu hooków + surowe upserty w Direction omijały cache i psuły spójność z Oracle `goal_spine`. | Jeden SSOT: `goalSpine.ts` (fetch/save + invalidate). Prowadzenie: `deriveSpineGuidance()` → `SpineGuideStrip` na zakładce Dziś, priorytet: sprint → tydzień → refleksja → KPI → 5 zwycięstw. |
| 2026-06-29 | useDirectionContext infinite loop | `onGoalSpineInvalidated(() => reload, [reload])` + `checkpoints ?? { all: [] }` (nowa referencja co render) → `useUpcomingCheckpoints` setState w pętli → Maximum update depth. Brak `sprint_reviews` / `primary_skill_id` wywala fetch. | `useGoalSpineInvalidation` (ref, deps `[]`); stały `EMPTY_CHECKPOINTS`; `useMemo` na return; `loadSprintReview` graceful; fallback select projects bez `primary_skill_id`. |
| 2026-06-29 | Codebase audit + dead code removal (~10k linii) | Wzorzec: budujesz wariant feature'a (np. `GrowthOverview.tsx`, `WorkoutQuickCapture.tsx`, 8 komponentów `/rozwoj` naraz, `WeeklyAnalytics.tsx`), w tym samym lub kolejnym commicie podłączasz inny wariant (`GrowthCockpit`, `WorkoutLogger`, `Direction.tsx`+submoduły), a przegrany plik zostaje w repo — nigdy nie był importowany albo stał się martwy z dnia na dzień. ~26 plików tak znaleziono, część z datą modyfikacji 1-3 dni przed audytem (świeża data ≠ żywy kod, sprawdzaj `git log --diff-filter=A` i pełny `git log --all -p` po nazwie komponentu, nie tylko obecny stan). Zewnętrzny audyt ("mimo") miał ~65% trafności — np. fałszywie oznaczył `web-push` jako nieużywane (było importowane), pomylił `src/lib/database.types.ts` (żywy) z `supabase/functions/_shared/database.types.ts` (martwy, inny plik o tej samej nazwie), i przesadził z "KRYTYCZNY wyciek credentials" dla plików będących w `.gitignore` i nigdy niewpychanych do git. | (1) Po wybraniu zwycięskiego wariantu komponentu — usuń przegrany w TYM SAMYM commicie, nie "później". (2) Zewnętrzny audyt zawsze weryfikuj grep'em/`tsc`/`git log --all` przed usunięciem czegokolwiek — nie ufaj samym liczbom linii/severity. (3) `unwrap()`/`unwrapList()` nie nadają się do `.maybeSingle()` (null = legalny wynik) ani do miejsc z silent-fail `if (!error)` — tylko do jawnego `throw` + bezwarunkowego użycia `data`. |
| 2026-06-30 | Dokończenie migracji `project_checkpoints` → `todo_items` (fallback z wpisu wyżej w tej samej tabeli, retired) | Audyt P0-P2 sprzątania wykazał, że "fallback na zawsze" z poprzedniego wpisu (15 żywych wierszy `project_checkpoints`, trzymanych tylko przez `checkpoints.ts`/`projects.ts`/`projectEvidence.ts`) to dokładnie ten typ stanu, który zewnętrzny raport pomylił z martwym kodem. Przy dropowaniu schematu dropnąłem też `daily_wins.task_N_checkpoint_id` na podstawie `count(*) where ... is not null = 0` — ale ta kolumna była AKTYWNIE zapisywana przez `PowerList.tsx` (checkpoint-driven daily plan proposal, `markCheckpointDone`), po prostu obecnie zawsze NULL w danych usera. `tsc` złapał to dopiero pośrednio (błąd w `PowerList.tsx` przy excess-property-check), nie wprost. | (1) Backfill + weryfikacja przed dropem: `INSERT ... SELECT` z id zachowanym 1:1, potwierdzone `count` po obu stronach, dopiero potem `DROP TABLE`. (2) **"0 wierszy z wartością" ≠ "martwa kolumna"** — zawsze grep całego `src/` po nazwie kolumny (nie tylko `select count(*) where x is not null`) przed `ALTER TABLE DROP COLUMN`; dane mogą być puste bo feature jest rzadko używany, nie bo jest martwy. (3) Po naprawie: odtworzona kolumna z FK do `todo_items` zamiast usuniętej `project_checkpoints` — okazja do poprawienia referencji przy okazji odzyskiwania z błędu. |
| 2026-07-02 | Przegląd inspiracji, wersja 4 — reorganizacja po architekturze | Wersja 3 dalej grupowała po aplikacji źródłowej (Motion, Reclaim, Logseq jako nagłówki sekcji) mimo poprawy konkretu silnika. User dał własny backlog S/A/B/C sortowany wg innego kryterium: ile modułów jedna mechanika wzmacnia jednocześnie (goal spine, Telegram, Oracle, Memex, Oura/Strava/kalendarz), nie z jakiej apki pochodzi. Całkiem pominąłem kategorię RoutineFlow (interaktywne rutyny w Telegramie) mimo że Telegram jest już żywym drugim interfejsem — najbardziej oczywiste rozszerzenie z całej listy. | Gdy user daje własną strukturę priorytetyzacji (tiery, kryterium sortowania) — użyć JEJ jako osi organizującej dokument, nie mojej wcześniejszej (app-based). Sprawdzić czy istniejący kanał wejścia (Telegram, stream) daje tanie rozszerzenie zanim zaproponuje się nowy ekran/UI. `docs/direction/FEATURE_INSPIRATIONS.md` v4: S tier = cykl życia zadania (rollover→pruning) + realny silnik planowania + Telegram routines + analityka nawyków; każda pozycja ma kolumnę "wzmacnia" pokazującą cross-module leverage. |
| 2026-07-02 | Przegląd inspiracji, wersja 3 po feedbacku "syfskie nie praktyczne" | Nawet po uchyleniu scope-gates (patrz wpis niżej) dalej hedge'owałem silnik Motion/Reclaim jako "tylko pokazuje dane, user decyduje" zamiast realnego enginu który przelicza/wstawia/tnie plan, i odrzuciłem Logseq/Tanę w całości jako "nowy edytor = scope creep" zamiast poszukać taniej wersji (block refs/backlinks/live queries nad istniejącym stream+graph, bez nowego UI notatek). Dorzuciłem też szum (finanse, Milanote, Matter, Notion Calendar) który rozwadniał dokument. | Gdy user mówi "zabij starą zasadę", sprawdź czy nie protegujesz tej samej ostrożności pod inną nazwą (evidence-only display zamiast realnej akcji). "Odrzucone bo drogie" wymaga sprawdzenia czy jest tańsza wersja tej samej idei, nie tylko odrzucenia całej kategorii. `docs/direction/FEATURE_INSPIRATIONS.md` v3: silnik faktycznie działa (capacity zmienia skład planu, buffery są wstawiane, replanning realnie przelicza dzień), triggerowany na otwarcie/komendę żeby nie łamać osobnej zasady "zero push-alertów" — ale sam silnik jest identyczny z Motion. |
| 2026-07-02 | Przegląd inspiracji z zewnętrznych apek (Motion/Reclaim/Logseq/Exist...) | Surowa lista "30 apek × 10 mechanik" wygląda atrakcyjnie, ale ~80% mechanik odpada na pierwszym pytaniu feature gate z PRODUCT_PRINCIPLES (gamifikacja, auto-scheduling, coach mode, prewencja). Część "nowych" pomysłów już istniała w kodzie (korelacje = lepszy Exist; stream = logseqowe daily notes; recovery/readiness liczone od dawna w compute-daily-strain, tylko nie wpięte w dailyPlanProposal). | Zewnętrzne inspiracje najpierw grep'ować po własnym repo (czy odpowiednik już nie istnieje), dopiero potem priorytetyzować. **Update tego samego dnia:** Jakub uchylił scope-gates konstytucji (coach mode / prediction / gamification / auto-scheduling dozwolone) — patrz banner w `PRODUCT_PRINCIPLES.md` i roadmapa Tier 1-2 w `docs/direction/FEATURE_INSPIRATIONS.md`. Guardraile epistemiczne (confirmed gate, inference nie mutuje evidence, source+count) zostają; "zero alertów prewencyjnych" zostaje do jawnego odwołania. |
| 2026-07-01 | Reorganizacja wizualna i nawigacyjna (Frontend 2.0) | Przełączanie widoków oparte na warunkowym montowaniu (np. ternary dla sub-tabów) oraz leniwe ładowanie głównych ekranów przy pierwszym kliknięciu wymusza ponowne pobieranie danych API i wywołuje irytujące plansze "Ładowanie...". Rozwiązaniem jest pre-montowanie wszystkich tabów głównych przy starcie oraz przełączanie podzakładek w oparciu o klasy CSS (`hidden`) bez ich odmontowywania. | (1) Aby zapewnić płynność 0ms i brak ekranów ładowania, pre-montuj krytyczne widoki główne (np. w Dashboardzie) oraz używaj klas CSS `hidden` do chowania podzakładek zamiast unmountowania. (2) Nie wciskaj desktopowych widoków standalone z paskiem bocznym (jak Todo) do mobilnych tabów. (3) Ożywiaj pasywne widoki (np. Projekty), osadzając na ich szczycie dynamiczne, powiązane z nimi priorytetowe akcje (np. 3 pilne zadania z opcją click-to-complete). |
| 2026-07-04 | Audyt duplikacji danych po Faza 4+5/Todo Faza 1-3/Weekly Review Wizard | Ten sam wzorzec z lessons.md (raw `supabase.from()` zamiast kanonicznego helpera, brak sprawdzania `{error}`) wrócił w DWÓCH nowych plikach naraz mimo że incydent już raz opisano: `MorningPlanModal.tsx` pisał do `daily_wins` z pominięciem `updateDailyWin`/`insertDailyWin` (brak `invalidateGoalSpineCache()`), a `WeeklyReviewModal.tsx` update'ował `todo_items` w `Promise.all` bez sprawdzania błędu + miał własną inline `getTodayWarsaw()` (dokładnie ten "70× copy-paste" bug z 2026-06-20) + `.order('position', ...)` na `todo_sections`, kolumnie która W OGÓLE NIE ISTNIEJE (jest `sort_order`) — sekcje zawsze wychodziły puste, błąd cichy bo złapany w `catch`. Dodatkowo `WeeklyReviewModal` (Dashboard) loguje ukończenie tylko jako marker w `vanguard_stream`, nigdy nie czytany z powrotem — karta "Tygodniowy Przegląd Zadań" nie miała pamięci i nagabywała ponownie tego samego dnia. Osobno: `TodoCard.tsx` ma DWA żywe magazyny podzadań (checklist w `notes` + realne `parent_task_id` children) i licznik postępu liczył tylko jeden z nich. | (1) Nowe featury pisane "od zera" trzeba jawnie sprawdzać pod kątem znanych wzorców z tego pliku PRZED review, nie tylko po zgłoszeniu — grep `supabase.from(` w nowych komponentach i porównać z listą kanonicznych helperów (`goalSpine.mutations.ts`, `lib/todo.ts`, `lib/date.ts`). (2) Migracja/schemat: sprawdzaj realne nazwy kolumn (`database.types.ts`) przed pisaniem `.order()`/`.eq()` na hardkodowanej nazwie — Supabase nie rzuca widocznego błędu w UI, tylko cichy `catch`. (3) Jeśli event completion jest zapisywany gdziekolwiek (nawet jako log/marker) i UI ma na tej podstawie NIE pokazywać czegoś ponownie — musi być też odczyt z tego samego źródła; sam zapis bez odczytu to atrapa. (4) Gdy dwa mechanizmy przechowują ten sam koncept równolegle (subtasks w notes vs parent_task_id) i migracja danych jest ryzykowna — bezpieczniejszy pierwszy krok to naprawić stronę odczytu (liczyć z obu źródeł), nie ruszać danych, dopóki user nie zdecyduje o docelowym scaleniu. |
