# Stan frontendu Vanguard — 11.07.2026, pełny obraz

> Migawka na dziś. Trzy plany, jeden wspólny cel: kod trudny do zepsucia, łatwy do edycji.
> Backend świadomie poza zakresem (decyzja: ten tydzień = tylko frontend).

---

## ✅ ZROBIONE I ZWERYFIKOWANE (można budować na tym bezpiecznie)

### FRONTEND_10_10_PLAN.md — S0-S14 — **zamknięte w całości**
- Ratchet v2 (liczniki wzorców) egzekwowane w CI
- `sanitizeHtml` — allowlista schematów, testy wektorów XSS
- Strefy czasowe (`getWarsawOffset`, DST test)
- `[Background Error]` — 0, wszystkie 54 wystąpienia przeklasyfikowane
- `lib/` — 83→31 plików root, 8 podfolderów wg domeny
- `hooks/` — 34→9 plików, reszta w folderach feature
- `desktop/` — 27 płaskich plików → 5 podfolderów (shell/hero/fitness/health/vision/general)
- Dokumentacja (`FRONTEND_GUIDE.md`) zsynchronizowana z kodem

**Commit:** `903a4a8d`

### DESIGN_SYSTEM_10_10.md — DS0, DS1 (częściowo), DS2, DS3, DS5 — **zamknięte**
- DS0: `ui/Spinner.tsx`, `ui/EmptyState.tsx`, `ui/ConfirmDialog.tsx` + testy + infrastruktura testowa React (nie istniała wcześniej w repo)
- DS1: 7 modali → `ui/Modal`, spinnery/empty states częściowo zmigrowane
- DS2: `index.css` 2358→419 linii — **cel osiągnięty**
- DS3: **0 hardkodowanych kolorów** (`bg-[#`, `dark:bg-[#`, `text-[#`) — zweryfikowane świeżym grepem, w pełni czyste
- DS5: `ui/Skeleton.tsx`, `ui/Badge.tsx`, `ui/Tabs.tsx` + testy

**Commity:** `97fd837e`, `efe1477d`, `e574e7bd`, `e26788dc`

### Spłata długu — P1, P2, P3, P4 — **zamknięte, liczby zweryfikowane**

| Tracker | Przed dziś | Po P1-P4 | Δ |
|---|---|---|---|
| `fixed inset-0` | 18 | 11 | -7 |
| `session: Session` prop | 44 | 28 | -16 |
| `as any` | 119 | 59 | -60 |
| `maxWarnings` | 659 | 496 | -163 |

**Commity:** `2b6e9b02`, `794baf08`, `06a81fc5`, `8137b4bd`

### P5 — 1 z 5 plików
- ✅ `MorningPlanModal.tsx` (829→219 linii, 7 nowych plików) — **znaleziono i naprawiono 1 realną regresję** (zgubione `notify()` w catch-u ładowania danych, niezłapane przez typecheck/lint/107 testów)

**Commit:** `4035c423`

### Bonusowe fixy znalezione po drodze (poza planem, ale realne bugi produkcyjne)
- `notes.css` — brakujący `@reference` psuł **całą zakładkę Keep** w dev (crash do ErrorBoundary)
- `TodoSidebar.tsx` — sidebar nie chował się na mobile, ściskał treść Todo do ~145px na 375px

---

## 🟡 W TOKU / CZĘŚCIOWE (nie licz jako gotowe)

| Wątek | Stan | Ile zostało |
|---|---|---|
| **P5** | 1/5 plików | `CalendarGrid.tsx` (najwyższe ryzyko), `LinksInbox.tsx`, `TodoCard.tsx` (wymaga planu do akceptacji). `RichEditor.tsx` — świadomy wyjątek na stałe |
| **DS1** | modale częściowo | **9 plików nadal ma ręczny `fixed inset-0`**: DailyShutdownModal, DashboardFastCapture, MorningPlanModal (backdrop, nie sam wizard), FoodEntryModal, SearchModal, InsightCard, EndMyopiaCalculator, ActionCenterSheet, WeeklyReviewModal |
| **DS1** | spinnery | **25 plików** nadal ma `animate-spin` poza `ui/Spinner.tsx` — część to legalne ikony Lucide (RefreshCw/Loader2), nie wszystkie są prawdziwymi kandydatami, wymaga oceny per plik |
| **DS1** | empty states | **30 plików** nadal ma `border-dashed` poza `ui/EmptyState.tsx`/`todo/EmptyState.tsx` |
| **DS4** | audyt responsywności | Sprawdzone ręcznie ~6 z ~10+ głównych widoków (Kalendarz, Todo, Keep, Rozwój, Projekty, Dziś). Desktop, Medical, Settings, Growth (do końca) — nie sprawdzone |

---

## ⬜ NIEZACZĘTE

### P6 — reszta `LEGACY_FILES` (~111 plików)

| Tier | Ile plików | Zakres linii | Charakter pracy |
|---|---|---|---|
| Tier 1 (mechaniczny) | ~87 | 300-400 | Jedno wydzielenie, batch po 3/commit |
| Tier 2 (umiarkowany) | ~21 | 400-600 | Prawdziwy split, 1 plik/commit |
| Tier 3 (ostrożny) | ~8 | 600+ | Jak P5 — plan do akceptacji, wizualna weryfikacja |

Największe pojedyncze pliki czekające: `exportStats.ts` (928), `usePowerListData.ts` (745),
plus 3 pozostałe z P5.

### DS4 — dokończenie audytu (Desktop, Growth do końca, Medical, Settings)

### DS6 — zasada skauta, z definicji nigdy "gotowe" — ciągła adopcja przy okazji

### Osobny dokument `POLISH_10_10_PLAN.md`
Ktoś (prawdopodobnie MIMO) napisał równoległy plan częściowo pokrywający się z P1-P6
(sekcje PD1-PD6) + sekcję P0 (backend krytyczny — **świadomie poza zakresem tego tygodnia**
wg Twojej decyzji). Nie przeczytany w całości, nie zatwierdzony — wymaga decyzji czy
scalić z P6 czy zignorować jako duplikat.

---

## Realistyczna skala

- **Zrobione dziś:** ~15 commitów, S0-S14 (14 sesji) + DS0/DS1(częściowo)/DS2/DS3/DS5
  (5 sesji) + P1-P4 (4 sesje) + P5 (1/5 plików) = **solidny, ale częściowy dzień**
- **Zostało realnie:** P5 (3 pliki wysokiego ryzyka), P6 (~111 plików, realistycznie
  wielodniowa/wielogodzinna praca nawet równolegle), DS1 dokończenie (9+25+30 miejsc),
  DS4 dokończenie (4+ widoki)
- **To nie jest "jeszcze trochę"** — to jest porównywalny wolumen do tego co już zrobiono
  dzisiaj, może większy, bo P6 (111 plików) nie miało jeszcze żadnej sesji
