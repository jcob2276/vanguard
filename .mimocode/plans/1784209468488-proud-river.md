# Refaktoryzacja frontendu src_new/

## Kontekst
Refaktoryzacja warstwy frontendowej w `src_new/` na gałęzi `feature/clean-structure`. Kod źródłowy to aplikacja React 19 + TypeScript 6 + Ant Design 6 + TanStack Query 5 + Tailwind CSS. Build via Vite. Branch: `feature/clean-structure`.

---

## ETAP 1: Oczyszczenie App.tsx i MainLayout.tsx

### 1a. Utwórz `src_new/hooks/useOnboarding.ts`
Nowy hook komponujący cały stan onboardingowy:

```ts
// Eksport: useOnboarding(profile: Profile | null)
// Zwraca: { showGdpr, showGuide, loading, handleAcceptGdpr, handleCompleteGuide }
```

Podać do niego logikę z `AppLayout` (App.tsx:64-155):
- Stan `showGdpr`, `showGuide`, `loading`
- `useEffect` sprawdzający `profile.gdpr_accepted`, `profile.onboarding_completed`, `localStorage`
- `handleAcceptGdpr` — update `profiles` w Supabase (`gdpr_accepted: true`)
- `handleCompleteGuide` — update `profiles` w Supabase (`onboarding_completed: true`) + localStorage

Importy needed: `useState`, `useEffect` from react, `supabase` from `../lib/supabase`, `useAuth` z kontekstu (ale auth jest już po stronie rodzica — hook przyjmuje `profile` jako parametr).

### 1b. Zmodyfikuj `src_new/App.tsx`
- Zaimportuj `useOnboarding` z `./hooks/useOnboarding`
- W `AppLayout`: zamień ręczny stan + useEffect + handlers na `const { showGdpr, showGuide, loading, handleAcceptGdpr, handleCompleteGuide } = useOnboarding(profile)`
- Usuń importy `supabase` z `./lib/supabase` (jeśli nie używane nigdzie więcej w pliku — sprawdzić: `supabase` jest używane tylko w `handleAcceptGdpr` i `handleCompleteGuide`, więc po ekstrakcji do hooka można usunąć)
- Nie ruszać logiki chunk error handler (lines 96-119) — zostaje w AppLayout

### 1c. Zmodyfikuj `src_new/components/MainLayout.tsx`
- **Usunąć** linijkę 19: `const isDtmsAdmin = user?.email === 'fhudtms@poczta.fm';`
- **Zastąpić** warunek na liniach 52 i 84/116:
  - `profile?.rola === 'admin'` zamiast `(profile?.rola === 'admin' || user?.email === 'fhudtms@poczta.fm')`
  - Avatar: `profile?.rola === 'admin'` zamiast `isDtmsAdmin`
  - Modal: analogicznie `profile?.rola === 'admin'`
- Usunąć niepotrzebny import `useAuth` (nie — `useAuth` jest potrzebne do `user`, `profile`, `logout` — zostawiamy)
- Zmienić: avatar src logic: `profile?.rola === 'admin' ? '/logoadmin.webp' : undefined` (2 miejsca)

**Plik do modyfikacji:** `src_new/components/MainLayout.tsx`

---

## ETAP 2: Porządki w module wyników (features/results/)

### 2a. Utwórz `src_new/features/results/components/ResultsKpiCards.tsx`
Komponent prezentacyjny (propsy: `stats: ResultsStats`).
Wyciąć z Results.tsx linie 178-227 (karty statystyk: Skuteczność, Egzaminy, Najlepszy, Centrum Porad).
Props:
```ts
type ResultsKpiCardsProps = {
  stats: ResultsStats;
}
```

### 2b. Utwórz `src_new/features/results/components/ExamHistoryTable.tsx`
Komponent prezentacyjny (propsy: wyniki, paginacja, kolumny).
Wyciąć z Results.tsx linie 231-264 (tabela + empty state).
Props:
```ts
type ExamHistoryTableProps = {
  results: ResultRow[];
  loading: boolean;
  currentPage: number;
  pageSize: number;
  total: number;
  onPageChange: (page: number) => void;
}
```

### 2c. Zmodyfikuj `src_new/pages/Results.tsx`
- Zaimportować `ResultsKpiCards` i `ExamHistoryTable` z `../features/results/components/`
- Zamienić inline JSX na użycie tych komponentów
- Zostawić w Results.tsx: hooki (useQuery, useWrongQuestionIds), nawigację, logikę startWrongReview
- `WrongQuestionsCollapse` — NIE tworzymy osobnego komponentu bo w obecnym Results.tsx nie ma akordeonu dla błędnych pytań — jest tylko przycisk "Powtórzy błędy". Pomijamy ten punkt zgodnie z faktyczną strukturą kodu.

---

## ETAP 3: Porządki w module ustnym (features/oral/)

### 3a. Utwórz `src_new/features/oral/types.ts`
Wyekstrahować typy używane w module oral:
```ts
export type { OralItem } from './components/OralQuestionCard';  // re-export istniejącego typu
// + TreeNode, Tab (z PracticalVademecum)
```

Definicje:
```ts
export type Tab = 'questions' | 'practical' | 'video';

export interface TreeNode {
  item: OralItem;
  number: number;
  children: OralItem[];
}
```

Re-export `OralItem` z `OralQuestionCard.tsx` (istniejący tam interface).

### 3b. Utwórz `src_new/features/oral/hooks/useOralVademecum.ts`
Hook pobierający dane i budujący drzewo:
```ts
export function useOralVademecum(categoryId: string | undefined) {
  // Zwraca: { items, category, questionTree, practicalTree, videoTree, isLoading, childrenByParent }
}
```

Przenieść logikę z `PracticalVademecum.tsx` linie 53-100:
- `useQuery` dla `oral-questions`
- `useQuery` dla `category-name`
- `childrenByParent` useMemo
- `questionTree`, `practicalTree`, `videoTree` useMemo
- Helpery: `buildTree`, `matchesQuery`

### 3c. Utwórz `src_new/features/oral/components/VademecumSidebar.tsx`
Wyodrębnić lewy panel nawigacyjny (sticky bar z tabami + szukajką) z `PracticalVademecum.tsx` linie 163-216.
Props:
```ts
type VademecumSidebarProps = {
  tab: Tab;
  onTabChange: (tab: Tab) => void;
  search: string;
  onSearchChange: (value: string) => void;
  questionTreeCount: number;
  practicalTreeCount: number;
  videoTreeCount: number;
  allExpanded: boolean;
  onToggleAll: () => void;
  isDarkMode: boolean;
}
```

### 3d. Zmodyfikuj `src_new/pages/PracticalVademecum.tsx`
- Zaimportować hook `useOralVademecum` i `VademecumSidebar`
- Usunąć zduplikowaną logikę pobierania danych i budowania drzewa
- Zastąpić sticky bar komponentem `VademecumSidebar`
- Zostawić rendering treści (tabs: questions/practical/video) w pliku

### 3e. Zmodyfikuj `src_new/pages/IIWjoOralMvp.tsx`
- Typować parametry `pickRandomIndex` (linia 26-33): `(items: Lesson[], currentIndex: number) => number`
- Typować `persistRepeatIds`, `openLesson` callbacki
- Sprawdzić czy są lokalne duplikacje z `features/oral/` — w tym pliku logika jest statyczna (iiWjoOralMvpLessons), nie ma overlapu z supabase oral. Wystarczy dodać typy.

---

## ETAP 4: Rozbicie panelu Admina (AdminOralQuestionsView.tsx)

### 4a. Utwórz `src_new/features/admin/components/AdminOralQuestionsTable.tsx`
Wyodrębnić tabelę + expandedRowRender z `AdminOralQuestionsView.tsx`:
- Linie 217-324: columns def + expandedRowRender
- Wymagane propsy: `parents`, `childrenMap`, `parentIndex`, `expandedRowKeys`, `onExpand`, `openEdit`, `openNewSubpoint`, `deleteMutation`, `isLoading`
- Typ: `AdminOralQuestion`, `SimpleCategory` re-export z widoku

### 4b. Utwórz `src_new/features/admin/components/OralQuestionFormModal.tsx`
Wyodrębnić modal formularza:
- Linie 406-515: Modal + Form
- Wymagane propsy: `open`, `onCancel`, `onSubmit`, `isPending`, `title`, `lockedParentId`, `editingItem`, `oralQuestions`, `categories`, `form`, `parentIndex`, `childrenMap`, `handleUpload`, `handleVideoUpload`, `videoUploading`

### 4c. Zmodyfikuj `src_new/features/admin/views/AdminOralQuestionsView.tsx`
- Zaimportować `AdminOralQuestionsTable` i `OralQuestionFormModal`
- Zostawić: hooki (useQuery, useMutation), stan (isModalOpen, selectedCategory, form), logikę openEdit/openNewParent/openNewSubpoint, handleUpload/handleVideoUpload
- Zastąpić inline JSX tabeli i modala użyciem nowych komponentów

---

## ETAP 5: Rozbicie Quiz Engine

### 5a. Utwórz `src_new/features/quiz/hooks/useQuizModals.ts`
Wyodrębnić stan modali z `useQuizState.ts`:
```ts
export function useQuizModals() {
  // Zwraca: { isReportModalVisible, setIsReportModalVisible, isImageModalVisible, activeImagePath, openImageModal, closeImageModal }
}
```

Przenieść z `useQuizState.ts` linie 43-45 (state) i 267-275 (callbacks).

### 5b. Zmodyfikuj `src_new/features/quiz/hooks/useQuizState.ts`
- Zaimportować `useQuizModals`
- Usunąć stany: `isReportModalVisible`, `isImageModalVisible`, `activeImagePath`
- Usunąć callbacki: `openImageModal`, `closeImageModal`
- Dodać do returnu: `...useQuizModals()` lub zdestrukturyzować
- Zwrócić nowe nazwy z hooka modali

### 5c. Utwórz `src_new/features/quiz/components/QuestionImage.tsx`
Komponent obrazu pytania z przyciskiem zoom:
- Wyodrębnić z `QuestionCard.tsx` linie 123-152 (image area)
- Props: `imagePath`, `onOpenImage`, `isDarkMode`

### 5d. Utwórz `src_new/features/quiz/components/AnswersGroup.tsx`
Komponent grupy 4 odpowiedzi (Radio.Group):
- Wyodrębnić z `QuestionCard.tsx` linie 206-267
- Props: `currentQ`, `userAns`, `failedAnswers`, `mode`, `showFeedback`, `highlightCorrect`, `isDarkMode`, `normCorrect`, `handleAnswer`
- Eksportować helper `normalizeCorrectAnswer(q: QuizQuestion): AnswerKey | null`

### 5e. Utwórz `src_new/features/quiz/components/ExplanationBox.tsx`
Box z wyjaśnieniem po poprawnej odpowiedzi:
- Wyodrębnić z `QuestionCard.tsx` linie 270-284
- Props: `explanation: string | null`, `visible: boolean`

### 5f. Zmodyfikuj `src_new/features/quiz/components/QuestionCard.tsx`
- Zaimportować `QuestionImage`, `AnswersGroup`, `ExplanationBox`
- Zamienić inline JSX na użycie podkomponentów
- Zostawić: header z numerem pytania, star button, debug info, report button

---

## Kolejność realizacji
1. ETAP 1 (App.tsx + MainLayout) — niezależny
2. ETAP 2 (Results) — niezależny
3. ETAP 3 (Oral) — niezależny
4. ETAP 4 (Admin) — niezależny
5. ETAP 5 (Quiz) — niezależny

Etapami 1-4 można pracować równolegle. Etap 5 też niezależny.

## Weryfikacja
Po każdym etapie:
1. `npm run typecheck` — zero błędów TypeScript
2. `npm run build` — kompilacja Vite exit code 0
3. `npm run test` — testy przechodzą (testy są w `src_new/` i `src_new/features/`)

Kryteria sukcesu:
- Żadnych zmian w `src/`
- Żadnych zmian w `supabase/`
- Build przechodzi
- Testy przechodzą
- Brak regresji funkcjonalnej
