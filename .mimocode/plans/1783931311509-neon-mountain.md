# Co zostało do zrobienia — pełny obraz

## Zrobione (w tej sesji i poprzednich)

- **Faza A** (Modale — Phase 1-3): Modal.tsx, Card.tsx, index.css tokeny, animate-scaleUp ✅
- **Faza B** (Button/Fab + ConfirmDialog/ToastHost/Tabs/EmptyState/Skeleton): ui/Button.tsx, Fab.tsx, ConfirmDialog, ToastHost, Badge ✅
- **Faza C** (Sweep 16 domen): 20+ plików → Button, 8 plików → Spinner, 7 plików → Skeleton ✅

## Zostało — Frontend UI (priorytet高→低)

### 1. DS1: Empty states — 27 plików z `border-dashed` (inline, nie EmptyState)
Nie wszystkie są kandydatami — wiele to intentional dashed-border (np. drag-target w Todo, sekcje w Calendar). Do sprawdzenia per plik: które z nich to faktycznie "pusty stan z ikoną + tekstem + akcją" → EmptyState, a które to只是 styled containers.

### 2. DS1: Zostało ~10 raw buttonów z `fixed inset-0` overlayami (_BACKLOG.md DS1)
Pliki: DailyShutdownModal, DashboardFastCapture, FoodEntryModal, ActionCenterSheet. Te mają własne overlaye, ale niektóre (np. DailyShutdownModal) mogłyby użyć ui/Modal. Wymaga analizy per plik.

### 3. DS4: Audyt responsywności — 6 obszarów do sprawdzenia
Desktop, Growth, Medical, Settings, Tydzień, Historia. Dla każdego: 375px i 1280px, horizontal overflow, console errors, touch targets.

### 4. PD1: `as any` — top offender `exportStats.ts` (60 occurrences)
Pozostałe: `useDirection.ts` (10), `offlineQueue.ts` (4), `foodLogging.ts` (3), `aiContext.ts` (3).

### 5. PD4/P5: God-file splits — 4/5 największych zrobionych, ~111 mniejszych (P6) nie zaczęte
Mechaniczna klasyfikacja Tier 1/2/3 → batchowe rozbijanie.

### 6. DS6: Adopcja ciągła — zasada skauta
Dotykasz pliku z inline empty state / hardkodem koloru → przy okazji przepnij na wspólny komponent/token.

## Zostało — Backend / Architektura (BACKLOG.md Części I-III)

### P0 — Blokery (BACKLOG.md §P0)
- P0.1: Nightly gubi wzorce (status "hypothesis" vs CHECK constraint)
- P0.2: Brak walidacji tokenu na verify_jwt=false
- P0.3: vanguard_graph_cleanup niszczy claims

### Część II — Knowledge Leverage (BACKLOG.md §1.1-§6.2)
- §1.1: Write-back proposed_memory do grafu (największa dźwignia)
- §1.2: Kalibracja prognoz (metryka nadrzędna)
- §1.3: Pompa active learning
- §2.1-§2.3: Rada Oracle, backtester, nightly ledger
- §4.1-§4.9: Graf wiedzy (claims, encje, bitemporalność)

### Część III — Bugi techniczne
- Write-orphany: 9 tabele z czytaniem bez zapisu
- Ominięte helpery: todo_items, kpi_entries
- Offline queue: brakuje pokrycia dla dreams/calendar/KPI

## Rekomendacja: następny krok

**Najlepszy następny krok to PD1 (`as any` cleanup)** — szczególnie `exportStats.ts` (60 occurrences). To czysty, powtarzalny pattern, jeden plik, zero ryzyka regresji wizualnej, a efekt obniża `as any` count o ~50%.

Alternatywnie: jeśli chcesz kontynuować UI alignment → DS4 (responsywność) lub DS1 (empty states audit).
