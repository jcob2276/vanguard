# Legacy workout frontend

React + Vite dashboard from the original fitness-tracking app. Tables: `workout_*`.

**Vanguard OS** (daily loop, stream, oracle, planning) lives in **Telegram + Supabase edge functions** — not here. See `AGENTS.md` and `docs/vanguard-core.md`.

Still used for: dashboard widgets, Oura/Yazio sync UI, Oracle chat (`MentorChat.jsx` → `vanguard-oracle`), workout logging.
